#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

function run_journalnode() {
  while [ true ] ; do
    $DIR/hdfs journalnode
    sleep 10
  done
}

$DIR/hdfs-mesos-env
run_journalnode
